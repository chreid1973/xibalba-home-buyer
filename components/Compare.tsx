import React from 'react';

// Placeholder for a feature to compare multiple property analyses side-by-side.

const Compare: React.FC = () => {
    return (
        <div>
            <h1 className="text-3xl font-bold">Compare Properties</h1>
            <p className="mt-4 text-slate-300">Select analyses from your dashboard to compare.</p>
        </div>
    );
};

export default Compare;
